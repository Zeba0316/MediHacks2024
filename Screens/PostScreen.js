import { View, Text } from 'react-native'
import React from 'react'

const PostScreen = ({ id, name, userImageName, title, description, isAnonymous, imageSent, imageName }) => {
    return (
        <View>
            <Text>PostScreen</Text>
        </View>
    )
}

export default PostScreen